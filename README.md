# flux-angular-presentation
Quick application to demonstrate using the Flux architecture in an Angular app.

To run.
1. Clone the repo locally
2. Run 'npm install'
3. Run 'npm start'

Tests can be run through 'npm test'
